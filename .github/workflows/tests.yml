name: Code Quality and Tests

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -e ".[dev]"
        
    - name: Run tests
      run: pytest -v -m "not requires_database and not requires_credentials and not requires_network"
      # Skip tests that require external dependencies (database files, credentials, network access)
      # Only run tests that can execute with the packages and code available in CI environment
      
    - name: Run ruff check
      run: ruff check .
      
    - name: Run ruff format check
      run: ruff format --check .